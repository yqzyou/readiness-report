# Postgres connection string (Vercel Postgres injects this automatically).
# Leave unset locally to use the default JSON-file storage backend.
POSTGRES_URL=

# Rate limiting (optional, defaults shown)
RATE_LIMIT_MAX=5
RATE_LIMIT_WINDOW_MS=60000
